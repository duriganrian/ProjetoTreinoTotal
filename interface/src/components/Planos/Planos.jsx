import './Planos.css'; // Importa um arquivo CSS para estilizar o componente.

function Planos() {
    return (
        <>
        </> // Retorna um fragmento vazio (sem conteúdo a ser renderizado).
    );
}

export default Planos; // Exporta o componente Planos para uso em outras partes da aplicação.
